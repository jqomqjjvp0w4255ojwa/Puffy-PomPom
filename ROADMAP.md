# 糰糰未來規劃筆記

> 純構想記錄，尚未實作。供之後逐步討論、設計、動工時參考。

---

## 1.阿居（即時討論劇情的對象）

**動機**：不想因為看不懂糰糰的行為（比如為什麼不吃飯），就要把整段對話手動搬去給別的 AI 看。想要一個能直接讀取「當前頁面」的旁觀者，跟玩家一樣是個看客，不必懂糰糰的全部歷史。

**形式**：
- UI 比照租車網站的「網站小幫手」模式：在便利貼（筆記）icon 旁邊加一個笑臉／對話泡泡 icon，放在書頁外面左下角，避免擋到既有版面。
- 人設可自訂，目前構想：叫 **阿居**，性格冷靜。

**技術構想**：
- 用便宜的 Gemini（Flash）。
- 不需要玩家手動複製對話，而是後端提供一個「當前狀態快照」端點（糰糰狀態、最近摘要、房間環境等），讓 Monday 自動讀取作為 context。
- 規模要控制：用既有的 day-summary / week-summary 機制做精簡上下文，避免把所有原始日誌塞進 prompt。

**待決定**：
- Monday 要不要記住「上次聊到哪」，還是每次重新讀取當前狀態（無記憶、純旁觀者）？
- 觸發機制：常駐泡泡，還是點開才載入資料？

**額度防護設計（動工前必須先做，避免擠爆 Gemini 額度）**：
- 阿居跟電視/音響台共用同一份每日呼叫統計，能看出阿居上線後會不會把電視台原本的額度也擠爆。
- 阿居自己設一個每日呼叫上限（後台可調），超過就不再呼叫 Gemini，改回固定台詞（例如「今天聊得有點多，明天再聊吧」）。
- 同一次對話要有冷卻時間，避免玩家連續快速發送把當天額度燒完。
- 撞到429時走跟電視台一樣的退場邏輯：回固定台詞（例如「阿居好像在發呆，等一下再說話」），不要讓聊天UI直接噴錯誤訊息。

**狀態**：先擱置，待後續排期再動工。

---

## 2. 照片分享（變相日記 + 圖像認知小遊戲）

**動機**：想跟糰糰分享照片，本質上是把這個系統變成自己的視覺日記。

**現實限制**：技術上可行（多模態模型能「看」照片），但成本上不行（vision API + 圖床費用）。

**保留的折衷構想（尚不實作）**：
- 做成**獨立頁面的小遊戲**，而不是直接整合進日常生態系統。
- 玩家上傳照片 → 由 Gemini 判斷圖片內容 → 把「認知結果」餵給糰糰 → 糰糰吐出內心飄渺跳脫的想法，或展開一段「糰糰（腦內）走進這張照片的小冒險」敘事。
- 重點是趣味性與不確定性，每次結果可能都不一樣，類似抽卡/ 隨機小品文。

**待決定**：
- 圖床方案（GitHub repo 直接存 / 外部圖床服務）。
- 是否要把這些「冒險敘事」存檔成一種特殊日記類型，回顧時能重新看到。

---

## 3. 資源使用的軟性後果機制（原構想：經濟系統）

**真實動機（重新定調後）**：起點其實是玩家自己現實生活中的焦慮感——比如「冰箱門開太久，現實裡電費會爆，所以不敢一直開」。原本想做「經濟系統」，但討論後發現核心需求不是金錢/購物模擬，而是**把現實裡資源消耗的潛意識焦慮，投射成糰糰房間裡的一種「軟性後果」**。

**設計原則（重要）**：
- **不做數字儀表板**（不需要「電費：128元」這種介面），而是用**敘事後果**取代量化系統——玩家不需要看到具體數字，只需要「感覺到」某些行為有重量。
- 例如：冰箱開太久 → 糰糰可能會抱怨冷、某個東西可能會壞掉、房間氣氛可能變了，而不是跳出一個帳單。
- 這個方向跟既有的「猜測優先」世界觀是相容的：玩家依然看不到後台的數值運算，只看到行為與敘事上的回饋。

**糰糰本身已有的樸素交換邏輯（線索）**：
- 玩家給「很強的風」，糰糰就幫忙「趕走三顆灰塵」——顯示糰糰心智中本就存在某種「付出/回饋」邏輯，是物理現象之間的隱喻交換，不是金錢。

**好玩的衍生想法（先記錄，未評估）**：
- 能不能想辦法「從小黑影身上弄點錢/資源出來」——小黑影本身設定上比較陰暗、感覺可以做點「犯罪」的事。如果要往這方向發展，玩家跟小黑影的互動可以是這個「軟性後果系統」裡比較刺激／灰色地帶的支線（比如某種有風險、有代價的資源來源，跟跟糰糰的正當交換邏輯形成對比）。

**需要先討論清楚才能往下走的問題**：
1. 哪些「現實焦慮」值得映射進房間？（電費/開冰箱只是一個例子，還有沒有其他類似的「我在現實會緊張的小事」？）
2. 後果要怎麼呈現？是糰糰的台詞/心情變化，還是環境本身的變化（比如東西變舊、變壞）？
3. 玩家要不要有「主動觸發」的入口，還是純粹被動承受（開了就開了，看糰糰怎麼反應）？
4. 小黑影的灰色資源支線要不要做成正式機制，還是先留著當有趣的腦洞？

**目前判斷**：方向已經比之前清楚很多，不再是「最大最模糊」的一塊，但仍建議在第 4 點（糰寶庫）有具體雛形後再回頭，因為「後果」很可能要透過糰糰的窩/收藏狀態變化來呈現，會用到同一套底層機制。

---

## 4. 糰寶庫（收藏癖系統）＋ 糰窩 ＋ 糰建設 —— 糰糰自己的「文明」

**動機**：白糰糰會替物品賦予感情，喜歡到處撿東西收藏。這不只是個寵物收集清單，而更像是糰糰自己在經營一個玩家看不懂、看不到全貌的隱性世界——「糰糰在我房間不只是存在，而是在建立一個我不明白的文明」。

**目前構想的世界觀（重要！這是核心定調）**：
- 糰糰有一個**看不見的背包（毛層）**，可以從裡面掏出東西進行交換或使用。
- 糰糰有**不同的窩**，每個窩裡也有自己的寶藏。
- 糰糰能**自己調度資源**——不是玩家在管理庫存，而是糰糰在背後自主決策、搬移、交換、收藏。
- 類比上更接近「文明 / 世紀帝國」式的資源調度系統，而不是單純的寵物收藏清單。
- 三個子系統的分工：**糰寶庫**＝物品本體（是什麼、從哪來、有什麼屬性），**糰窩**＝物品存放的地點容器（在哪、會不會消失），**糰建設**＝糰糰拿物品做出來的「成果」（行為籤庫裡 `invent` 類型已經有「把垃圾堆疊排列、自認在發明文明」的素材，這條線就是把那些隨機行為升級成有記憶、會累積的具體產出）。

**整體已決定（四個關鍵問題拍板結果，動工時照這個方向設計）**：
1. **資料結構**：物品/窩/建設各是**三個獨立資料表**（`world.hoard.items` / `world.hoard.nests` / `world.hoard.constructions`），彼此用 id 互相參照（item 記 `nestId`、construction 記用到哪些 `itemIds` 與所在 `nestId`），不互相內嵌複製資料。
2. **玩家介入**：開放**間接提示**入口——玩家可以「丟一個東西」或對某個地點做動作，糰糰會把這個丟出來的線索納入考量，但最終要不要收進寶庫、要不要用在建設上，由糰糰（AI）自己決定，不是玩家直接下指令操作資料。
3. **窩的時間衰減**：**第一版先不做自動消失**機制。先讓三表資料結構與 AI 互動迴圈跑起來，之後視實際運作狀況再回頭評估要不要加「太久沒踏足會消失」這個收尾機制（避免在還沒驗證好玩之前，先卡在「門檻怎麼定」這個次要問題上）。
4. **數量上限**：物品/窩/建設都**設總量上限**，逼糰糰自己做取捨決策（捨棄舊的換新的）——這個「取捨瞬間」本身要當成敘事素材，AI 寫場景時可以順勢帶出糰糰猶豫/割捨的小心情。

### 4-1. 糰寶庫（物品本體）

**喜歡撿的東西清單**（具體素材，已有）：
- 冰棒棍／絲襪纖維／紙條／塑膠環／小襪子／奇怪石頭

**核心要求：開放詞彙，不是寫死清單**：
- 當糰糰撿到一個**從沒被定義過的新東西**（例如「阿里巴巴的背毛」這種莫名其妙、聽都沒聽過的物品名），AI 要能**自己判斷、自己賦予臨時屬性**，並存進寶庫——而不是出錯或忽略。
- 意味著物品資料結構不能只存「名字」，至少要有一個讓 AI 即興填的「屬性/感情標籤」欄位（例如質地、糰糰對它的感情強度、用途傾向），新物品第一次出現時由 AI 順手生成這些欄位，之後重複出現就直接讀取，不用每次重新判斷。

**草擬資料結構（等動工時再細修）**：
```
world.hoard.items = [
  { id, name, firstSeenAt, source(撿到的情境/地點), nestId(目前放在哪個窩),
    tags: [自由文字，AI生成，如「黏黏的」「捨不得換出去」],
    affinity: 數值或文字（糰糰對它的依戀程度，可能會隨時間/互動變化）}
]
```

**已決定**：物品設**總量上限**（具體數字等動工時依實際素材量再定，例如先試 10-15 件），滿了之後糰糰撿到新東西要先決定捨棄舊的哪一件——這個決策過程本身要寫進場景敘事。

**待決定**：
- `affinity` 要不要是會變動的數值（類似熟悉度/好感度那套衰減/成長機制），還是固定標籤就好？
- 玩家「丟東西給糰糰」的間接提示要怎麼觸發（現有的哪個輸入入口可以重用，還是要新增一個）？

### 4-2. 糰窩（存放地點容器）

**機制**：
- 記錄「進行過築窩儀式」的地點（行為籤庫的 `nest` 類型已有對應的築窩動作素材，可以直接當「成立新窩」的觸發來源）。
- 每個窩底下掛著屬於這個窩的物品清單（指向糰寶庫的 item id，不是複製一份資料）。
- **第一版不做時間衰減**：窩成立後先不會自動消失，等其他機制都跑順了再回頭評估要不要加「太久沒踏足會消失」這個收尾規則。

**已決定**：窩的數量**設上限**（例如先試 2-3 個），滿了之後糰糰要新築窩，得先決定放棄/合併哪個舊窩——跟糰寶庫一樣，這個取捨過程要寫進敘事。

**待決定**：
- 窩的數量上限具體要設幾個比較剛好（等真正動工、有實際素材量再試）。
- 之後要加時間衰減時，門檻怎麼跟現有的安靜時段/tick頻率換算（先記錄這個問題，不卡現在動工）。

### 4-3. 糰建設（拿物品做出來的成果）

**動機**：行為籤庫 `invent` 類型目前只是隨機抽到的單次小動作（「把雜物堆疊排列，自認在發明糰式文明」），抽完就結束、不會累積。這條線要把它升級成「有記憶的建設」——糰糰搭出來的東西會留著、被記錄、甚至被自己拆掉重搭。

**構想**：
- 一個「建設」＝消耗/佔用糰寶庫裡的某些物品＋發生在某個糰窩（或房間某處），成立後會持續存在，直到糰糰自己決定拆掉或太久沒去看而荒廢。
- AI 生成時可以參考「糰糰目前有沒有正在進行/已完成的建設」，讓敘事有累積感（不是每次都從零開始發明新東西，而是「他又去調整上次搭的那個歪掉的小堆疊」）。
- 跟糰窩的關係：**建設是獨立資料表**（`world.hoard.constructions`），用 `nestId` 參照所在窩、用 `itemIds` 參照用到的物品，不內嵌複製資料（對應整體已決定的第1點）。

**已決定**：建設也設**數量上限**（例如先試每個窩 1-2 個建設），滿了要新建設，糰糰得先決定拆掉哪個舊的——拆除瞬間是很好的敘事素材。

**待決定**：
- 建設的數量上限具體要設幾個（跟窩一樣，等動工時再試）。
- 要不要對應到第5點的成就系統（例如「第一次成功的建設」解鎖一張相簿照片）？

**AI 互動迴圈（讓整套系統「活起來」的關鍵，不只是資料庫）**：
- AI 生成糰糰行為時，會參考他的物品庫、偏好、目前窩與建設的狀態，影響決策（例如：他有沒有喜歡的毛？這週纖維夠不夠？要不要把某個東西換出去？要不要去整理上次沒搭完的堆疊？）。
- 玩家通常看不到糰糰調度資源的全貌，只能透過糰糰展示出來的行為／敘述片段去推測——維持「文明感」與神秘感。

**技術上可參考**：
- 跟既有的 `hidden-stats.json` / `events.json` 的時間戳衰減模式很像，技術上可以複用類似的機制（如「太久沒踏足會消失」）。
- 行為籤庫（`data/actions.json`）的 `hoard`/`nest`/`invent` 類型已經有大量素材可以當這三塊系統的「動作觸發來源」，不需要另開一套新的動作清單，先把既有素材接上資料結構即可。

**整體已決定**：四個關鍵問題（資料結構/玩家介入/時間衰減/數量上限）已在本節開頭拍板，三塊可以一起動工，不用再等。

**動工前最後還要敲的細節**：
1. 各表的數量上限具體數字（物品/窩/建設各設幾個比較剛好）。
2. 玩家「丟東西給糰糰」的間接提示，要重用現有的哪個輸入入口（或新增一個）。
3. `affinity`（物品依戀度）要不要做成會隨時間/互動變動的數值。

---

## 5. 成就系統（手繪/像素照片收藏，低調觸發）

**動機**：不是劇情向的視覺小說，而是想要「達成某些條件可以收集糰糰照片」＋「隨機小彩蛋事件」，例如：糰糰爬上床、插上竹籤、宣布這裡是他的窩，玩家不能上來了。

**核心設計原則**：
- **不彈窗、不強調「成就達成」**——低調寫入筆記或紀錄裡一筆，玩家自己翻到才會發現，符合糰糰世界「猜測優先」的調性。
- 觸發來源兩種：
  1. **數值型**：靠既有 hidden-stats（熟悉度／好感度等）算出的條件達成（例如熟悉度首次破某個門檻）。
  2. **隨機彩蛋型**：獨立一份條目清單，給低機率 roll，類似 events.json 現有的特殊事件結構，但不需要數值門檻、純隨機觸發。
- **照片**：玩家未來會自己手繪或做成像素動圖，不靠 AI 生圖。系統現在只需要存「成就 id + 時間 + 觸發當下的文字描述」，圖片之後再手動對應補上即可，不卡現在的開發。

**儲存位置（已決定）**：複用既有「筆記」分頁的書櫃圖鑑系統（`data/fragments.json` 的 chapter/items 結構），開一本新書冊（例如《糰糰相簿》），成就照片＝這本書裡的條目。差異只是觸發方式從關鍵字隨機機率，改成數值門檻/彩蛋直接解鎖；條目多留一個圖片欄位給之後手繪/像素圖用。不需要另開新頁面或新 UI。

**已決定**：成就/彩蛋的文字記錄不進主敘事資料流（不影響 day/week summary 與 AI 記憶），只存成獨立的書冊條目。若彩蛋本身是「糰糰做的一件事」，可以留一筆極簡場景描述讓玩家當下看到，但跟「成就解鎖」這個後設標記分開存。

**待討論的細節**：
- 數值型成就的門檻清單（哪些 hidden-stats 條件值得設成就點）。
- 隨機彩蛋的條目清單與大致機率。
- 資料結構：`world.achievements`（陣列，記 id/time/text）+ 對應 unlock 邏輯放在 tick 裡還是事件系統裡。

---

## 6. 寵物監視器（數值驅動的視覺化糰糰）

**動機**：目前整個系統是純文字敘事，這個構想要做一個固定視窗、可左右平移的「監視器畫面」，把糰糰目前的各種數值即時轉換成視覺呈現，而不只是文字描述。

**構想（目前偏簡單版本）**：
- 畫面裡的糰糰由數值計算呈現幾種狀態，數值包含物理性與抽象兩類：
  - 物理性：例如房間 X 軸座標決定糰糰在畫面中的左右位置。
  - 抽象：例如心情高→毛變澎（變大顆）、飢餓→來回走動。
- 玩家可以左右平移視窗去找糰糰目前在房間的哪個位置。
- 參考來源：使用者另一個倉庫「iceland」的 `index.html` 有類似的圖像呈現方式可以參考（**目前這個 session 的 GitHub 存取範圍只開放 puffy-pompom，看不到 iceland 倉庫內容，需要使用者之後直接貼程式碼/截圖或開放該倉庫存取才能具體比對做法**）。

**長期野心（先記錄，不評估）**：
- 想像中最終要做成更複雜的「生態箱」——不只是顯示幾個簡單狀態，可能要呈現環境互動、多種行為動畫等更豐富的畫面。

**跟糰糰相簿系統的連動構想**：
- 監視器是「持續呈現」，相簿（第5點的成就書冊）是「精選快照」。
- 監視器畫面裡出現的某些特殊瞬間（例如誇張姿態、罕見行為組合）可以做成相簿解鎖的判定條件之一，把兩個系統串起來。

**技術門檻評估**：
- 比現有任何功能都重，需要額外的 2D 渲染層（CSS 或 Canvas），把數值映射成視覺參數（位置/體積/動作頻率），是目前 ROADMAP 中技術量級最大的一項。
- 建議先做最簡化版本驗證「看起來像糰糰」這個核心樂趣點（例如純 CSS 變形的圓形毛球＋幾個數值映射），不要一開始就衝著「生態箱」的複雜度去設計。

**具體技術參考（已取得，來自使用者另一個專案 iceland-budget 的 index.html / scene.js）**：
這份專案用純 CSS + SVG + Canvas（無框架）做出一個有質感的像素風即時場景，手法直接可以借用：
- **固定畫布**：一個 `overflow:hidden` 的場景容器，裡面疊一層 canvas（畫天氣粒子效果，如雨/雪/霧）＋絕對定位的 SVG 角色。
- **位置映射**：角色用 `left: X%` 定位，搭配 `transition: left .8s steps(12)` 做出逐格移動的像素感動畫（不是平滑滑動）。原專案是用「花費佔預算百分比」算出 `left%`，糰糰可以換成「房間 X 軸座標」。
- **狀態整批切換**：用一個函式統一控制背景色/粒子效果，依狀態類型整批換視覺（原專案是天氣類型），糰糰這邊可以對應心情/健康狀態整批切換（例如灰塵粒子效果可以跟小黑影設定結合）。
- **角色變形**：用切換 CSS class（例如 `.dead` 旋轉 SVG）表現極端狀態，沒有用複雜骨骼動畫，對「開心變澎、餓了走動」這類簡單狀態切換已經足夠。
- **對話泡泡**：絕對定位 div + opacity transition 輪播文字，可以直接借來做糰糰的狀態小語。

結論：技術門檻比預期低很多，這個案例證明「純 CSS+SVG+canvas、無框架」就能做出有質感的像素場景，不需要額外的渲染引擎或框架。

**進階版構想（使用者補充：固定視窗尺寸＋攝影機式環繞）**：
跟上面的「簡單版」不同之處在於——畫面尺寸固定不變，但**攝影機本身可以左右（甚至上下）平移**，去房間裡「找」糰糰，而不是糰糰在一個固定畫面裡走來走去。糰糰會依數值或關鍵字動態切換**多套像素動圖**（不只是換位置，連動作/姿態都換）。這比簡單版多了兩個維度的複雜度：(1) 房間比視窗大，需要鏡頭跟隨/搜尋邏輯；(2) 糰糰不是單一 sprite 切 class，而是多套動圖之間的切換。

**分階段提案**：
- **階段 0（驗證視覺風格）**：先用簡單版的固定鏡頭＋單一 CSS 變形圓球，確認「看起來像糰糰」的基本樂趣點，不做鏡頭移動。
- **階段 1（鏡頭可平移，糰糰維持單一 sprite）**：房間寬度設定為視窗的 2-3 倍（例如用一個比視窗寬的 `.room` 容器），玩家用左右按鈕或拖曳/滑動讓 `.room` 的 `translateX` 移動，糰糰本身座標對應房間座標、視窗只是這個房間的一個「窗口」。糰糰先維持像簡單版一樣只用 CSS class 切換狀態（不澎/澎、走/不走），不用多套動圖。技術上等於把 iceland 那套 `left%` 定位機制，從「相對視窗」改成「相對一個更大的房間容器」，鏡頭再疊一層 transform。
- **階段 2（多套像素動圖，依數值/關鍵字切換）**：等階段 1 的鏡頭機制穩了，再处理「換動作」這件事——例如準備幾組小尺寸 sprite-sheet 或多張 SVG/png 動圖（站立、走動、捲成一團睡覺、爬高、生氣鼓起來等），用一個狀態判斷函式（類似 iceland 的 `setSky()`）決定目前該播哪一套，依據可以是 hidden-stats 數值區間，也可以是 AI 輸出文字裡偵測到的關鍵字（例如場景文字提到「爬上床」就觸發對應動圖＋瞬間定位到床的座標）。這一階段的工作量主要在「素材準備」（要幾套動圖）跟「狀態→動圖的映射表」，渲染機制本身可以沿用階段 1 的鏡頭系統。
- **階段 3（上下平移／2.5D 房間）**：如果想要房間不只是一條長廊、而是有高度（例如糰糰可以在地面/床上/櫃子上），可以加第二軸的鏡頭平移，房間從一維長條變成二維平面（或用「Y 軸決定一個小幅度的縮放/上下位移」模擬簡易透視）。這個複雜度明顯比階段 1、2 高，建議只在前面階段都驗證好玩之後才考慮。

**再進階構想（使用者補充：套房多房間＋互動規則）**：
不只是一條長廊，而是一整套「套房」——房間/廁所/廚房/客廳/陽台等多個空間，甚至帶有 365 度環視、找房間/開門的密室逃脫式探索感。這比階段 3 又多了一層：不是單一房間的 2.5D 平移，而是**多個房間之間的切換/連通**，鏡頭從「移動」變成「移動＋換場景」。

延伸出的兩個獨立子構想：
- **天氣/窗外景色連動**：開關天氣會影響畫面中的物件與窗外景色（呼應 iceland 那套天氣粒子效果，可以直接複用：每個房間的窗戶是一塊獨立的小畫布，依當前天氣狀態套對應的粒子/色調）。這個跟「找糰糰」的鏡頭機制是分開的系統，可以獨立做、隨時插上去。
- **熟悉度驅動的互動規則**：熟悉度低時看糰糰、糰糰會跑走；摸他可能被「戳」一下（好感度倒扣）。這其實是把現有的 hidden-stats（熟悉度/好感度）邏輯，從「純文字判定」搬進「視覺化監視器」裡變成一種即時互動小遊戲——玩家的滑鼠/點擊行為本身會即時影響數值，不再只是被動看畫面。

**規模提醒**：這幾個構想合起來，已經從「一個視覺化監視器」變成「一個小型探索+互動遊戲」，工作量遠超過階段 0-3。建議的拆法是：天氣/窗景可以當成獨立的小特效模組先做（風險低、隨時能加）；多房間探索＋互動規則則建議等階段 1、2（單房間鏡頭+多動圖）穩定之後再評估要不要往這個方向擴，避免一開始就把監視器設計得太重，導致遲遲生不出第一個能玩的版本。

**已敲定的第一刀切法（純物件導向、暫不放糰糰）**：
- 形式：**單張長卷 2D 場景**（一張長圖含整套房所有擺設），左右平移（`translateX` + 邊界 clamp）即可看到全部房間，不做密室逃脫式的場景切換、也不做真正 360 度/球形環視——這就是楓之谷側捲軸那種「平移一張長圖」的難度，不需要鏡頭數學。
- 物件互動：場景裡的物件（例如陽台植物）**不切換到新場景**，而是「點擊→彈出近距離詳細視圖（lightbox/inset）」，看完關掉回到原本的捲動位置。之後想做澆水、生長階段之類的微互動，都掛在這個 detail view 裡，不需要重新設計成獨立房間。
- 物件導向的好處：先把「擺設」做成可定址的資料（id/位置/圖），日後購物系統可以直接往同一份清單塞新物件；糰糰沒有審美但東西一多就有更多「冒險素材」可用。
- 天氣連動、熟悉度互動（不熟會跑走/亂摸被戳扣好感）、多房間密室逃脫式探索，都先留在「待決定/規模提醒」裡當未來加碼選項，**不是第一刀的範圍**。
- 第一刀具體驗收標準：一張可左右平移的長圖＋至少 1 個可點擊出現 detail view 的物件（建議就用陽台植物），先不放糰糰、不做天氣、不做熟悉度互動。

**值得先想清楚的問題（影響後面好不好做）**：
- 「找糰糰」本身要不要變成一種輕量互動／小遊戲（例如鏡頭移動有阻力、或糰糰會主動躲起來），還是單純把它當成一個可以隨時逛逛看的監視器，沒有「目的」？
- 鏡頭移動方式：左右按鈕（離散步移，貼近像素風格）、拖曳/滑動（連續），還是兩者都做？
- 多套動圖先做幾種就夠用（建議先抓 3-5 種最常見/最有反差的狀態，例如：日常待機、開心毛澎、生氣鼓起、睡覺、走動），不需要一次覆蓋所有可能心情組合。

**待決定**：
- 簡化版的視覺呈現方式（CSS 變形 vs. 簡單 sprite 動畫 vs. 參考 iceland 倉庫的既有做法）。
- 數值→視覺參數的映射清單（先列 3-5 個最有趣的數值對應關係即可，不用一次做全）。
- 跟相簿系統串接的判定邏輯細節（等兩個系統都有雛形再談）。
- 進階版的鏡頭/房間尺寸設計要不要納入初版規劃，還是先做階段 0-1 驗證手感再決定要不要往階段 2、3 走。

---

## 7. 成長系統（月摘要記錄新習慣／新感受，標記關係的細膩變化）

**動機**：糰糰跟玩家的關係不是一條平的線，而是會隨時間累積出細微差異——玩家想要的不是數值上的「熟悉度+1」，而是敘事上能感覺到「這個月糰糰好像多了點什麼」：學會了新習慣、對某件事多了種以前沒有的感受、或者跟某個東西/角落的關係變得不一樣了。

**形式構想**：
- 在既有的 day-summary / week-summary 機制之上，加一層**月摘要**，除了濃縮這個月發生的事，額外標記「這個月糰糰有沒有長出新習慣，或學到新的感受」。
- 不是數值面板，而是延續既有「猜測優先」的調性——這些新習慣/新感受用敘事文字呈現，玩家自己從摘要裡讀出關係的變化，不是系統跳出來宣告「解鎖新行為」。
- 跟第5點（成就系統）的差別：成就是離散的「達成瞬間」，這個成長系統更像連續的「累積基調」，可能沒有明確的解鎖時刻，是月摘要回頭看才浮現的模式。

**待決定**：
- 月摘要要不要存成獨立資料（類似 `week-summaries.json` 的月版本），還是只是週摘要彙整時順便產生的衍生內容。
- 「新習慣／新感受」要怎麼判定要不要寫進月摘要——完全交給生成摘要的 AI 自己從這個月的日記裡歸納，還是要先有一些 hidden-stats 門檻/關鍵字輔助提示？
- 跟第4點（糰寶庫）、第5點（成就系統）是否會互相參照（例如某個新習慣剛好對應某個成就解鎖，月摘要要不要順便提一句）。

**目前判斷**：技術門檻不高（可以直接沿用 `ensureWeeklyDigest` 的模式往上疊一層月摘要），主要工作量在「怎麼讓 AI 可靠地從一個月的素材裡歸納出『真的有變化』的部分，而不是空泛地說『這個月很開心』」，建議跟第1點（阿居）一樣可以隨時找空檔先做雛形。

---

## 排序建議（暫定，可隨時調整）

目前認為**第 4 點（糰寶庫／文明系統）**概念最成熟、最契合既有世界觀，可以優先深入設計。
第 3 點（經濟系統）很大程度依賴第 4 點的物品/資源庫存基礎，建議在第 4 點有具體雛形後再回頭討論。
第 1 點（Monday）技術門檻最低，可以隨時找空檔先做。
第 2 點（照片小遊戲）目前已決定先擱置、僅記錄構想。
第 5 點（成就系統）門檻不高，可以跟第 1 點一起排在前面先做。
第 7 點（成長系統／月摘要）技術門檻也不高，沿用既有 week-summary 模式即可，可以跟第 1、5 點一起排在前面先做。
第 6 點（寵物監視器）目前技術量級最大，建議等第 4、5 點有雛形、且能參考到 iceland 倉庫做法後再評估時程；可以先做最簡化版本驗證樂趣點，不用一次衝著「生態箱」的複雜度設計。
